import { CategoryUpgradeStatements } from './category.upgrade.statements';

describe('CategoryUpgradeStatements', () => {
  it('should create an instance', () => {
    expect(new CategoryUpgradeStatements()).toBeTruthy();
  });
});